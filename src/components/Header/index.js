import React from 'react';
// import Nav from '../Nav';

const Header = (props) => {
return (
  <header class="general-flex"> 
    <div> 
        <h1>Thomas Ciszek's Portfolio</h1>
    </div>
    {/* <Nav>{props}</Nav> */}
</header>
);
}

export default Header;